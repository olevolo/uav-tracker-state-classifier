# APCESignal — Phase 5 (Option A: confidence proxy).
#
# Architect decision: degrade APCE to tracker confidence proxy for Phase 5
# since cv2.TrackerKCF does not expose the raw correlation response map.
# Authentic APCE (Cao 2025) requires the Henriques 2015 port (Phase 6+).
#
# value = 1 - confidence   (high signal → low confidence → may escalate)

name: apce
range: [0.0, 1.0]
args: {}
